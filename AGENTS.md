# Prototype Instructions

Full Case Forum with Floating Salon Gallery, AI Prompts & 3-Tab Push Transitions (2026-09-19, latest):
- Suspended Salon Gallery on Pink Canvas: restored the curated floating gallery of cases on the iconic AIQUOS brand pink (`#f568a3`) film-grain canvas (`/assets/case-poster-grain.png`). Each case is a suspended museum mount (`#fffdfb`) with subtle natural hanging angle (`hangAngle`), authentic 3D pointer-tracking perspective tilt (`perspective: 1200px`), postal stamp (`AuthorStamp`), category tag badge, gliding hover capsule (`查看案例与提示词 ↗`), and real-time upvote heart button.
- All Project Cases Integrated: all 17 real cases from AIQUOS (Wing It, Super Mario, Japanese Conbini, Pirate Ship, Penguin 3D, Moon Route PPT, Onboarding Schedule, Cardboard Astronaut Stop-Motion, Red Team Security Audit, Browser-Use Computer Control, Cat in Glasses, Parrot, Collage Poster, The Last Ice, Girl & White Cat, Fishbowl Avatar, Summer Bus) are woven into the forum database (`src/forum-topics.js`), divided cleanly across four core channels: `AI 生图`, `AI 视频`, `AI 代码`, `AI 办公`.
- AI Prompt & Case Detail Experience (`ForumDetail`):
  * Dedicated high-contrast **AI 核心提示词 · PROMPT** code card with model badge (`Midjourney v6.0`, `Runway Gen-2`, `Claude 3.5 Sonnet + Three.js`, etc.), parameter tags, and a working one-click **复制提示词** button with toast feedback.
  * Deep author reflection writeup (`案例配文与实践复盘`) explaining prompt engineering rationale, pitfalls encountered, and takeaways.
  * Real-time likes and personal center favorites integration.
  * Authentic community comments stream from mentors, plus working comment composer for immediate interaction.
- User Posting (`ForumComposer`): supports selecting `AI 生图` / `AI 视频` / `AI 代码` / `AI 办公`, inputting title, AI core prompt, model/tools, description writeup, and image upload/selection; newly published cases immediately append to the top of the floating gallery.
- Omnidirectional Home ↔ Cases ↔ Forum Push Transitions:
  * Full horizontal sliding push (`pushPages` with `SLIDE_DURATION = 760ms` and `SLIDE_EASING`) between all three tabs: Home ↔ Cases, Cases ↔ Forum, Home ↔ Forum.
  * Synchronous `flushSync(() => setPushing(true))` ensures sibling `.home-tab-screen.is-cases` and `.home-tab-screen.is-forum` layers exist in the DOM during the push, preventing blank frames or unmount flickers.
  * URL and view states commit synchronously upon arrival.

Authentic Community Forum Architecture and Layout Upgrade (2026-09-19, latest): the Forum tab is transformed from an art-gallery wall into a genuine, modern, enterprise-grade community forum (`GalleryBoard` in `src/forum-gallery.jsx` + `src/forum-board.css`):
- Community Hero Header: branded hero banner with kicker (`AIQUOS 智核社区 · AI 能力与实战讨论场`), bold heading (`真实任务研讨 · 实践经验沉淀`), subtitle, community stat pills (15 篇复盘 / 12 位导师 / 1.8w+ 互动), and a prominent `+ 发起新讨论` action button.
- Horizontal Channel Navigation: scrollable channel tabs for all 8 categories (`全部讨论`, `测评研究`, `实战案例`, `作品分享`, `学习笔记`, `前沿观察`, `讨论场`, `校园故事`, `效率工具`, `每周精选`) with live post count pills and topic dot indicators.
- Authentic Two-Column Layout (`.community-grid`):
  * Main Feed Column (left): toolbar with sorting tabs (`最新发布` / `最热讨论`), search input with instant clear, active filter chips bar, and thread stream (`.forum-thread-card`).
  * Real Thread Cards: author avatar, nickname, professional role badge (e.g. `AI 产品经理` / `数据可视化设计师`), timestamp, colored category tag badge, bold clickable title, 2-line excerpt summary, media thumbnail preview, and engagement bar with real-time interactive like counter, comment count, views, bookmarking, and `阅读讨论 ↗` button.
  * Rich Community Sidebar (right): 社区交流准则 guide widget, 热门讨论榜 (Trending Topics Top 5 with ranks and views, clicking instantly searches the topic), 活跃导师与贡献榜 (Top 5 mentors with avatars, names, titles, likes, clicking filters posts by author), and 实战小建议 quote card.
- Thread Detail View (`ForumDetail` in `src/ForumBoard.jsx`): enriched with breadcrumbs navigation (`智核社区 / 分类 / 讨论详情`), author profile header with title/role and engagement counts, key takeaway quote card, full multi-paragraph article body, big upvote CTA, live comments stream, and interactive comment composer with immediate submission.
- Maintained consistent AIQUOS signature pink canvas (`#f568a3`) with high-resolution film grain overlay (`/assets/case-poster-grain.png`), with pristine white cards (`#ffffff`) and soft shadows (`--shadow-1`, `--shadow-2`), and complete responsive collapse to single column on mobile (<860px) with zero horizontal overflow.

Forum tactile elevation and Home/Cases design alignment (2026-09-19, latest): the Forum tab is elevated to match the craftsmanship and tactile materiality of Home and Cases:
- Background & Materiality: overlaid the authentic high-resolution film grain texture (`/assets/case-poster-grain.png`, mix-blend-mode: overlay) over the entire Forum field, matching `case-poster::after`. Default field color set to the iconic AIQUOS brand pink `#f568a3` (`FORUM_DEFAULT_FIELD`), keeping it fully consistent with the Home canvas (`--pink`) and Cases (`HOME_GROUND = #f56ba3`) with subtle ambient radial lighting, smoothly tinting to topic colors when filtered.
- Hero & Wordmark: bilingual hero kicker (`AIQUOS COMMUNITY · 智核社区` / `TOPIC · [Name] · 主题展区`) with pulsing dot, Home-resonant subtitle (`用真实经验，沉淀 AI 实践认知。从提示词调优，到作品与经验复盘。`), and grand artistic `SplitWord` (`COMMUNITY` / topic words: `RESEARCH`, `CASEWORK`, `SHOWCASE`, `NOTES`, `OUTLOOK`...) with per-letter entry bounce.
- 3D Cursor-Tracking Perspective Tilt: every hung wall piece (`WallPiece`) now implements interactive 3D pointer tracking (`tiltPiece` setting `--piece-tilt-x`, `--piece-tilt-y`, `--piece-shift-x`, `--piece-shift-y` with `perspective: 1200px`), directly echoing CaseArchive's `tiltFeature`. Frame features museum passe-partout mount (`#fffdfb`), beveled inner mat (`#f4eef2`), authentic postal cancellation stamp (`AuthorStamp`), and an interactive `"OPEN POST / 阅读讨论 ↗"` capsule badge that glides up on hover (matching Case's `OPEN CASE`).
- Navigation & Paging: added floating side navigation arrows (`forum-side-arrow is-left/is-right` with `<CaretLeft/>` and `<CaretRight/>`) mirroring Cases' `case-poster-arrow`, interactive page pill indicators (`01`, `02`, `03`) echoing Home's carousel dots, and keyboard navigation (`ArrowLeft`/`ArrowRight` to flip pages).
- Top-Right Compose Pill: `.forum-compose-button` refined into an iconic AIQUOS white pill button (`+ 发布讨论`, bold 800 font, smooth hover elevation), matching Home's `AI测评` and `账号` buttons.
- Immersive Post Detail: `ForumDetail` upgraded with `.forum-detail-screen.is-immersive` carrying a blurred artwork backdrop (`--detail-art`, `--detail-ground`) and neutral radial veil layer (echoing `CaseDetail.is-immersive`), styled AIQUOS pill return button `← 返回论坛`, and interactive heartbeat like button.
- Clean responsive single-column collapse for viewports under 900px with zero horizontal overflow.

Admin console (2026-09-19, latest): a second Vite entry `admin.html` + `src/admin/` ships a management console at `/admin.html`, same design family as the student app (brand pink as the single accent, DM Sans + Noto Sans SC, tokens.css grade/elevation values; local demo passcode `admin`, front-end only). Views: 数据概览 (KPIs, grade distribution, six-dimension averages, 8-week trend, recent runs), 学员管理 (search/filter + detail drawer with radar), 题库管理 (audit panel + filter/search/paginate + edit dialog), 测评记录 (snapshot viewer), 系统设置. Roster = deterministic seeded cohort (演示班级, 24 students) merged with the machine's real runs as 本机学员 (`src/admin/cohort.js`; swap for an API when a backend exists). Bank editing is REAL: `worker/bank-store.js` wraps the bank behind an override-aware store; `PUT /api/admin/bank` validates with the vendored validator and ALWAYS publishes the next version (v6 -> v7; the bump is the vendor version contract, there is no no-bump save), persisted by the vite middleware to gitignored `worker/bank-overrides.json` (`setBankPersistence`; a deployed worker without injection keeps overrides in memory per isolate and reports `persistent: false`). `worker/comprehensive-quiz.js` serves through the store and returns `bankVersion`; the student app caches it (`aiquos.bank-version.v1`), re-labels in-progress attempts (`withBankVersion`), and validates drafts against the cached version — an admin publish cleanly supersedes stale drafts. History no longer filters by bank version (snapshots keep their recorded version; only scoringVersion mismatches are dropped) so old reports stay visible after publishes. Never add a "save without publish" path. Tests: `tests/admin.test.mjs` (store/serve/persistence/cohort/stats); E2E screenshots in `work/admin-qa/`.

Backend-authoritative adaptive serving (2026-09-19, latest): 综合测评 question selection moved off the client. `POST /api/comprehensive-question` (`worker/comprehensive-quiz.js`, wired in `worker/index.js` and the vite middleware) receives `{ levelId, stage, session?, exposure?, outcome?, debug? }` and returns `{ question, session, exposure, debug? }`. The worker imports the PURE engine (`src/comprehensive-adaptive.js`) and the single bank copy (`src/comprehensive-questions.json`) — the algorithm cannot drift between backend and tests; `tests/comprehensive-server.test.mjs` pins the contract (validation, parity with the src engine, full 25-question runs, no re-serves). The client (`SiteExperience.fetchComprehensiveQuestion`) only threads the opaque server session between requests plus the outcome of the question just answered; `ComprehensiveTask` fetches per stage entry with loading/error/retry states and an in-flight-promise guard so StrictMode's setup→cleanup→setup cycle reuses one request instead of burning two questions. Client bank import is REMOVED (`comprehensive-quiz.js` no longer exports `COMPREHENSIVE_QUESTIONS`; the 120-question JSON left the client bundle — do not re-import it client-side). Scoring stays client-side per the vendored package's integration guide; exposure counters still persist in `aiquos.adaptive-exposure.v1` and travel with each request (stateless worker). The attempt draft now carries `routing` (the server session) so a mid-run reload resumes without re-serving used questions — keep attaching it in `saveAttemptDraft` call sites. Starting/restarting a run nulls the session (fresh routing). Serving the answer key in the response matches the existing objective-API trust level (demo).

Adaptive engine v2 and optimization pass (2026-09-19, latest): `src/comprehensive-adaptive.js` now routes on real evidence. With no credited answers selection is byte-for-byte the old ordering; once credits flow (`controller.record(outcome, credit)`, credit from `answerCredit()`), a 1-parameter logistic ability estimate blends with the position walk in `nextTargetDifficulty`, per-dimension information weights (1/(1+count)) favour the least-measured dimensions, a hard guard prevents a third consecutive same-type question whenever the level offers an alternative, and persistent exposure counters (`aiquos.adaptive-exposure.v1`, capped penalty 0.12×min(6,uses)) demote over-served questions across runs. The hidden state still only chooses questions. `src/assessment-attempt.js` gained: memory-fallback storage (runs survive setItem throwing), a migrator-chain seam for future draft/history shape changes, FIFO eviction in `recordAnswer` when a resumed run re-enters finished stages and would exceed 25 unique answers (the most recent 25 are scored — this is also why map progress now initialises from the draft, so 继续测评 lands on the right stage), `answerCredit`, `historyAtCapacity`/`exportAttemptHistory`, and `clearAllAssessmentData` for the AccountSettings 数据与隐私 panel. Product additions: per-panel `ErrorBoundary` (`src/ErrorBoundary.jsx`, errors also land in an `aiquos.error-ring.v1` ring buffer); hash routing centralised in `src/routes.js` (`siteViewForHash` — keep new routes there); a draft resume banner on the comprehensive map (继续测评 goes to `current`, 重新开始 rebuilds the attempt); quiz keyboard answering (1-4/A-D select, Enter submits/continues — single/judge still commit on pick); the strip gained an in-progress note that also warns when remaining questions cannot cover the missing dimensions (completion is exactly 25 answered AND all six dims evidenced); type-rule hint in the quiz brief; 跳过对话 buttons on all story dialogues; report versions footnote (bank + scoring), a 分数如何得出 disclosure, a cap notice with JSON export when history holds 12, and a labelled demo fallback with a 去完成综合测评 CTA; the records view gained a last-4-runs compare table and token-coloured grade badges; `src/tokens.css` (grade colours S/A/B/C/D, elevation, radii) is imported first in main.jsx — new surfaces read grades/elevation from tokens; index.html carries a CSS-only boot splash removed by React's first render; `.github/workflows/ci.yml` runs tests, c8 coverage on the frozen-contract modules, and the build; the maintenance workflow lives in `.agents/skills/aiquos-assessment-steward/SKILL.md`; the 100-item roadmap with 30 checked priorities is `docs/optimization-roadmap-100.md`. Screenshots from the verification rounds are under `work/scoring-qa/`.

Six-dimension scoring integration (2026-09-19, latest): the 综合测评 now scores for real. The canonical engine from the supplied `aiquos-six-dimension-scoring` package lives unmodified at `vendor/aiquos-six-dimension-scoring/` (scoring version 1.0.0, difficulty anchors low/medium/high = -1/0/+1, N(0,1) posterior, 60-step bisection, grade bands S/A/B/C/D); import its pure ESM exports — never re-implement its formulas. `src/assessment-attempt.js` owns the Attempt lifecycle per the package's integration guide: the bank (`objective-bank-v6-120`, 120 questions) is validated once per run, every submission re-creates evidence from the served question and recomputes from the complete answer set, `totalQuestions` is 25 (5 stages x 5), and `completed` additionally requires evidence in all six dimensions. A card click starts a clean attempt (adaptive routing state and draft both reset); an unfinished draft persists in `localStorage` under `aiquos.comprehensive-attempt.v1` and tolerates a re-served question by replacing its evidence. Completing stage 5 stores the result exactly once as an immutable history snapshot (`aiquos.comprehensive-history.v1`, latest 12) and routes to `#reports` instead of the map. During a run the task view shows a 六维实时画像 strip — per-dimension live estimates with evidence only, no overall score or grade while in progress. `AwakeningReport` renders the latest completed snapshot (demo data, clearly labelled, only when no snapshot exists); CENTER 测评记录 gains real calendar records from history whose 觉醒报告 modal opens that exact snapshot. Both the reports page and ProfileDetail stay mounted while hidden, so they re-read history when they become active — do not move those reads back to mount-time initializers. The adaptive selector itself is unchanged: it still routes questions only.

Forum board revision (2026-09-19, latest): the community board is **展藏 / GALLERY** — white mounts hung at slight angles over a dark ink field `#17150f`, the newest post as the two-column lead, paged five to a page, labels under the frames, filters set into the foot of the field. The other two compositions from the three-way comparison (信箱 LETTERS postcard board, 期刊 JOURNAL index) and the bottom view switcher were removed at the user's decision; the board files are `src/forum-gallery.jsx` + `src/forum-board.css` (renamed from `forum-variants.css`), with shared state in `useForumBoard` (`src/forum-board.js`). Ground and word rules that remain: the unfiltered board opens on ink with the word GALLERY; a selected topic retints the field to the topic's sampled colour and swaps the word for the topic's own (RESEARCH / CASEWORK / …, map in `topicWord`); all type flips white/ink via `fieldInk()` and `data-ink`, and the header's nav labels follow the field through `useFieldInk` writing `.app[data-field-ink]`. The forum tab screen's top padding is `0` in both layouts — the field supplies its own and must start at the very top; its horizontal inset still comes from `--tab-screen-inset-x`, which every full-bleed child must cancel. Two standing cautions: (1) never give a full-bleed protagonist a pointer-following tilt — rotating toward the cursor moves the target away and the element becomes unclickable; (2) IntersectionObserver callbacks are fed by rendered frames, and embedded webviews stop rendering while occluded — any IO-driven reveal therefore needs the bounded failsafe `useReveal` now carries (settle everything after 2s), the same precedent as the homepage's 8-second boot curtain. `forum-effect.js` (RealitySplit) remains in the tree but unmounted; `forum.css` keeps the tokens (on `.forum-board-root`, `.forum-detail-screen`, `.forum-compose-screen`), card/cover primitives, detail split and composer.

Forum family revision (2026-09-18, latest): the Forum tab now opens like every other page in the product — a `FORUM!` wordmark (DM Sans 900, per-letter bounce; no bespoke artwork was supplied, so it is built from type, not a crop) over a hero stage, instead of the untitled paper canvas the old "Forum shows no kicker/title" note specified. **That earlier note is superseded.** The hero's ground is the brand pink `#f568a3` and retints to the selected topic's sampled colour, so the stage always matches the discussion being read; the wordmark, tagline and the header's nav labels all flip to white on dark fields and to ink on amber ones, because the hero scrolls up under the header and a fixed nav colour becomes unreadable there (`fieldInk()` in `ForumEffect.jsx` decides, `.app[data-field-ink]` carries it). Forum rules now live in `src/forum.css`, migrated out of `responsive.css` — the design tokens are declared on `.forum-tab`, `.forum-detail-screen` **and** `.forum-compose-screen`, because the detail and composer replace the board in the tree rather than nesting inside it. `forum-effect.js` gained `setField(color)`/`applyField()` (field colour only; the letter-card and handle palettes stay sampled), and `forum-card.jsx` / `forum-art.jsx` / `forum-toolbar.jsx` split the wall card, the geometric covers (the data's unused `art` field: dots/grid/arcs/waves/rings), and the topic/sort/search/member controls. The wall supports topic, member, keyword and 最新/最热 filtering; the detail ends in a same-topic related rail (rendered only when the host can open a post — the favorites centre cannot, so it gets no rail); the composer's left pane previews the real card as the reader types. Whatever bleeds full width on this tab must cancel `--tab-screen-inset-x`, and every layout that overrides the tab screen's horizontal padding must set that variable with it.

Comprehensive adaptive question revision (2026-09-11; its final no-scoring sentence is superseded by the 2026-09-19 six-dimension scoring integration below): apply adaptive question selection only to 综合测评. Keep every stage at five questions and preserve the existing answer feedback, story, map, and completion flow. Start the run around medium difficulty, carry the hidden routing state across all five stages, and partially regress it toward medium at each new stage. Select each next question only after the current response, balancing difficulty fit, under-covered ability dimensions, question-type variety, no-repeat constraints, and randomized exposure among similarly suitable candidates. The hidden state exists only to choose questions: do not calculate, display, persist, or feed a score, grade, radar chart, awakening report, or other assessment result.

Unified assessment stages (2026-09-06, latest): the comprehensive card is titled 综合测评 / Comprehensive Assessment. All four TEST cards now enter their own themed five-stage map; only `#assessment/<id>/level/<n>` is a task route. Every task exposes the top five-node progress trail and 返回关卡地图, uses the comprehensive task-panel sizing rules, returns to its map after each stage, and retains its own objective, conversation, or practical task content. This supersedes the earlier direct-run behavior and its 已完成 X 题 hint.

Homepage copy alignment revision (2026-09-06, latest): the Home intro heading, supporting copy, and AI测评 button form one right-anchored block. Desktop and compact layouts preserve their original type scale and vertical rhythm while positioning the block against the page's right margin; no copy or CTA route changes.

Avatar and record report revision (2026-09-06, latest): both CENTER 账号设置 and the homepage 账号 entry share the AccountSettings avatar uploader. A selected local image previews immediately in the profile card, replaces the nickname initial, remains local-only, and does not persist or transmit credentials or media. In CENTER 测评记录, a completed 综合能力闯关 record exposes 觉醒报告 and opens it as a local modal with radar, grade, scores, advice, and resources; scheduled future comprehensive records do not claim a report. The extracted report content continues to power the standalone #reports page without repeating its dialogue in the modal.

Organization class membership revision (2026-09-06, latest): the 我的组织 white rank chart keeps its light-bordered histogram/slider presentation with class-themed blue highlights. The header contains a local class-code lookup after the title; exact demo codes AI2026A, AI2026B, and DATA2026 expose a found-class card and 加入班级 action. Joining updates class identity, roster, score, average, and rank; 退出班级 clears membership into a local empty state. No backend or persistence is introduced.

Personal-center detail revision (2026-09-06, latest): the five CENTER cards open distinct assessment-style detail surfaces, not repeated blank-colored placeholders. Organization includes class information and the supplied histogram/slider rank pattern; Works uses expandable accordions; Records keeps the fixed 2026-09-06 two-column calendar; Favorites reuses Forum wall cards; Account settings remains a white profile/editor card. Case and Forum details expose in-memory favorite controls whose entries feed Favorites. Opening a favorite preserves its source detail pattern: case favorites render the standalone Cases detail layout, while forum favorites render the Forum detail with comments and likes. Navigation must parse the intended `#center/<id>` hash when entering a detail so the selected profile id stays synchronized.
Favorite detail chrome revision (2026-09-06, latest): Case and Forum favorite details carry the homepage viewport scale outside `.app`, so both use their original source layouts with `返回收藏` fixed at the top-right account-control position; compact favorite details follow the same source-detail compact rules without horizontal overflow.

Awakening report revision (2026-09-06, latest): the CHOOSE 报告查询 card opens a standalone `#reports` awakening-report surface instead of staying closed. It uses the assessment-screen anatomy with the horizontally centered Xiaoyuan dialogue immediately above the report content and no REPORT art wordmark; the first line and the one white result card are both visible on entry, and clicks reveal only the remaining Xiaoyuan sentences—never “系统展示” narration. Six-dimension radar, S/A/B/C/D grading, per-dimension scores, personalized suggestions, recommended resources, and local-only screenshot/print-PDF actions all live in that white card on the same pure green page. The reports screen scrolls without a visible scrollbar, its state resets when leaving the page, its back control is 返回选择, and no backend dependency is introduced.

Account and time revision (2026-09-06, latest): CENTER 账号设置 and the homepage 账号 entry share the same white profile-card layout with avatar, nickname, account, password, and local-only 退出登录. From CENTER the return control goes back to ProfileHub; from the homepage entry both return and logout go home. The records calendar treats 2026-09-06 as its fixed “today”, highlights that date, and generates later demo record/schedule dates relative to it.

Assessment records revision (2026-09-06, latest): the CENTER 测评记录 detail uses a two-column calendar screen. The left white panel follows the supplied August calendar style with month controls and dots on dates that have records; clicking a date immediately updates the right white panel with that date's local demo assessment records. Compact layouts stack the calendar above the record list while retaining the same interaction.

Comprehensive quiz polish (2026-09-05, latest): the comprehensive dialogue layer stays inside the task view so returning to TEST never leaves a character line visible. The desktop comprehensive panel is taller with a retained bottom shadow, and dialogue opens above the panel rather than over it. The first TEST card visibly reads 综合能力闯关 / Comprehensive Ability Challenge. Homepage's account pill sits in the header's top-right position, is white with black text, and sizes to its label.

Direct assessment runs (2026-09-05, latest): Objective, Conversation, and Practical cards never open a stage map. They enter question/task 1 immediately (or resume the next unfinished item), expose 返回测评选择 from the task, advance directly to the next item after completion, return to TEST after item five, and restart after a completed run. Their white panel shows a themed 已完成 X 题 hint above the task body while the comprehensive five-stage map is unchanged.

Media display revision (2026-09-05, latest): Forum wall card images use the fixed card width with proportional intrinsic height and no cropping. Forum detail images use full-visibility containment on a white gallery backing. A black rule separates gallery from post content on desktop and stacks as a horizontal rule on compact layouts. Transparent dark prev/next controls with a light halo sit on the image sides, and black dot indicators with white outlines remain below every post image (one dot for single-image posts). Direct objective, conversation, and practical assessments show the 已完成 X 题 pill at the top of the assessment screen, outside the white task panel.

Comprehensive dialogue and feedback revision (2026-09-05, latest): the TEST comprehensive card copy is 综合能力闯关. Its opening and ending dialogue is rendered above the white task panel as a portal-backed full-viewport click layer: the first line is visible immediately, each click reveals exactly one sentence, no explicit next button is shown, and any click advances the dialogue. The white task panel is taller and anchored to the bottom of the viewport; comprehensive submit/continue actions use the assessment page's rounded blue control language, remain uncompressed, and occupy their own full-width action row after feedback/reaction. Per-question feedback uses the same soft assessment-tinted rounded surface rather than the supplied file's black brutalist card, but must retain the source-inspired wrong-answer shake and correct-answer star pop/glow effects. The homepage account pill copy is 账号. Forum detail headers use a black rule between the title/summary area and the scrollable body; case-detail media is enlarged to `min(620px, 72dvh)` on desktop.

Comprehensive quiz revision (2026-09-05, latest): the TEST comprehensive assessment now follows the supplied `level.html` play pattern while retaining its blue background, TEST wordmark, white panel, guide art, and rounded control language. Its five stages map to 智核学院 / 信息迷城 / 创客工坊 / 协同空间站 / 伦理殿堂. Each stage uses five questions drawn from the local comprehensive question bank, supports single, multi, and judge questions with immediate submission for single/judge and explicit submission for multi, then shows correctness, full answer, analysis, dimensions, and a guardian reaction. The former top dialogue pane is removed: one click reveals only one story/reaction sentence at a time in the panel's own soft rounded style; opening lines precede questions and ending lines complete the stage. Do not reintroduce chat panes or backend dependencies for this prototype.

Case and forum media polish (2026-09-05, latest): the Cases stacked preview is enlarged to approximately 362×456 desktop viewport behavior (larger but bounded on compact screens) while retaining the same one-to-one row/image mapping and no remount churn. A Forum detail supports multiple left-side images with in-place opacity paging, side controls, and dots; all extra images can remain mounted. Its text/comment column scrolls with a hidden scrollbar while the composer and author/views/likes/comments action bar remain fixed at the detail's bottom.

Forum post wall revision (2026-09-05, latest): below the Forum hero, replace the old placeholder grid with a square-cornered CSS masonry wall of twelve local demo posts. Cards use varied-ratio images from the existing optimized case assets above their text, expose tag, summary, author, publish time, views, comments, and likes on their original colored card footer, and reveal sequential per-line title underlines on hover using Range-measured line boxes, matching the supplied sharing prototype. Clicking a card opens an immediate standalone Xiaohongshu-style post surface: desktop fills the viewport with image on the left and a scrollable text/comments column on the right, with no centered white detail card; compact screens stack media above the text. Only detail pages have the comment composer plus interactive author/views/likes/comments action bar. Every post supports in-memory like toggling and comment publishing, keeps those changes while navigating back to the wall, and must not use a modal or route into the separate CENTER/profile flow.

Detail return controls and Forum surface (2026-09-05, revised later): Cases and Forum detail views are standalone full-viewport surfaces without the shared navbar, AIQUOS brand, or account pill; opening a detail hides the tab host's outer padding. Their fixed 返回 controls occupy the top-right account-button position (`top: calc(24px * --ui-scale); right: calc(41px * --ui-scale)`, compact top `max(22px, safe-area)` and right `25px`). The Forum tab's full-screen background is its hero-animation paper color `#fff4fa`, and its navbar text plus compact brand are black while Cases remains white.

Cases archive revision (2026-09-05, latest): the Cases tab replaces its prior animation/card wall with a Work archive below the shared navbar. The homepage-hosted tab background remains `var(--pink)`, while the archive/list surface uses `#ffe3f4`. Its source images are the renamed sequential originals `assets-case/1.png` through `assets-case/36.png`, with optimized page copies `public/assets/cases/1.webp` through `public/assets/cases/36.webp`; rows and images map one-to-one and each row carries its archive number. The centered preview keeps all 36 image nodes mounted and only swaps front/lower/upper/back/hidden classes, so changing rows must not remount or re-fetch the deck. Stack images share one fixed height while width follows their intrinsic aspect ratio (viewport-bounded only). Clicking a row opens an immediate case-detail surface with the same image, an explanation, and a 返回列表 control; reduced churn matters more than re-triggering entrance animations. Both the Cases page scrollbar and its internal archive scrollbar are visually hidden; archive scrolling remains available.

Homepage settings CTA revision (2026-09-05, latest): the main navigation is Home / Cases / Forum only. The homepage's lower-left 探索案例 button is now AI测评, retains the original outgoing-arrow icon, and uses the established scroll-to-login behavior. The upper-right black pill is labeled `account`, matches the 返回首页 pill's 122×44 size, appears only on the Home tab (not Cases or Forum), and opens a standalone `#account-settings` screen with a direct 返回首页 control. That screen must not connect to, reuse navigation from, or be confused with the existing CENTER `#profile` page.

Homepage explore correction (2026-09-05, latest): the homepage 探索案例 button switches immediately to the Cases tab through the same `onCases` route as the navbar; it is disabled only while a transition is busy.

Login and Forum chrome revision (2026-09-05, revised later): the standalone login screen's surrounding stage is black (`#08080a`), while the original pink square form design remains unchanged. The Forum tab's navigation bar has no special black background; it uses the standard homepage navbar treatment.

Homepage tab backgrounds (2026-09-05, revised later): Cases, Forum, and Account tab screens use the homepage background `var(--pink)` (`#f568a3`). Their animation canvases do not inherit the page color: Cases keeps `#fff4fa`, Forum keeps `#fff4fa`, and the letter-card colors remain unchanged. Cases, Forum, and Account use dark ink copy where needed for readability, while their compact AIQUOS brand mark plus Home/Cases/Forum/Account links and active underlines are white. These are homepage-hosted tab-screen backgrounds and replace the previous shared orange wall color. The homepage navbar's visual treatment stays unchanged, but on Cases/Forum/Account it lives inside the scrollable tab content and scrolls away with the page.

Forum hero animation (2026-09-05, latest): the Forum tab places the supplied split/zoom canvas animation immediately below the nav text, with only the normal navbar-to-content breathing room, and above its card wall. It is full viewport width and 60dvh tall with a `36px` radius on all four corners. It cycles through share, discussion, member, online, public, community, and thread. Its canvas background is Forum's `#fff4fa` tab color across every cycle, while letter-card, handle, and ink colors keep the sampled CHOOSE, TEST, and CENTER card colors; zoomed letter cards are pre-rendered at final zoom density so they stay crisp. Forum shows no kicker/title, and its card wall is exactly two cards per row. This animation is Forum-only; Cases keeps its title wall.

Cases loud-burst revision (2026-09-05, latest): the Cases tab replaces the image galaxy with the supplied loud/burst canvas animation. It is full viewport width and `40dvh` tall with a `36px` radius on all four corners, immediately below the navbar and above the existing wall. Its paper background is `#fff4fa`. It cycles these four phrases: “representative AI use-cases”, “typical AI applications”, “successful AI deployments”, and “AI application scenarios”. Reduced motion presents the still frame.

Home/login transition revision (2026-09-05, revised later): Home ↔ login uses the browser's native View Transitions API for the vertical page scroll when available, so the live homepage is not cloned by hand and the animation starts with lower latency. The existing DOM snapshot fallback remains for engines without View Transitions; both paths keep the 840 ms smooth vertical motion.

Fallback transition responsiveness (2026-09-05, revised later): homepage scene capture only requests visible case layers; paused/hidden next-group layers no longer block the transition. The fallback holds the outgoing page immediately after capture, so neither direction exposes the live target before the shared scroll overlay starts.

AI-assessment login revision (2026-09-05, latest): the AI测评 CTA always performs the downward scroll into the standalone login form from any homepage-hosted tab (Home, Cases, Forum, or Account); it never checks a demo session or skips to CHOOSE. Remove all login-session expiry checks and the old demo-session storage helper. Login submission still moves to CHOOSE, while account/password inputs remain visual demo fields with no credential or password validation.

Motion and login-card revision (2026-09-05, latest): Home ↔ login uses the lighter virtual-scroll path: `freezeView` snapshots are used directly, the shared track runs on a two-frame-ready start with GPU-friendly 3D translation and an 840 ms smooth ease. The standalone login card is a large square (`640px` when the viewport allows; viewport-width/height bounded on smaller screens) while the original pink form design remains unchanged. The homepage bottom-right next-case chevron is removed; case selection is through dots/toggle only. CHOOSE ↔ CENTER/profile keeps the whole-card flight, but profile layers participate in the same transparent-background/hidden-back-control rules and use softer, longer card motion with increased incoming overlap.

Case-switch correction (2026-09-05, latest): do not use the body-diagonal cube turn for homepage case-group changes. Selecting a carousel preset or dot swaps the three face contents directly with the original screen-content method and does not rotate the shell. Keep the continuous calibrated three.js shell only for the CHOOSE → home return: after the reverse three-band pull lands, the same 900 ms calibrated flat-to-case rotation returns the homepage cube to case playback. Entering CHOOSE still does not rotate or visibly flatten the cube.

Transition correction (2026-09-04): TEST (`#assessments`) back control is labeled 返回选择 and returns to CHOOSE with the established staggered whole-card flight. The CENTER/profile page also enters from and returns to CHOOSE with that same card flight. The homepage shell's continuous calibrated three.js rendering remains available for the CHOOSE → home return, with live DOM screen quads driven by the same projection and the DOM-projection fallback when WebGL is unavailable. The WebGL texture source must use the complete `.cube-shell` rounded/base cutout (not the stale shorter mask), and WebGL materials discard translucent edge fringe so rotation never trails a detached pink edge.

Cube and homepage navigation revision (2026-09-04, revised 2026-09-05, later revised): entering CHOOSE never rotates or flattens the cube. Home → CHOOSE still uses the three-band pull measured at card whitespace; CHOOSE → home first reverses those bands and then runs the retained calibrated shell turn back to case playback. Case-group changes swap face contents directly and never rotate the physical shell. Cases, Forum, and Account are homepage-hosted navbar tabs: they keep the same pink reference canvas and header treatment, swap immediately without a page transition, hide the homepage hero/cube controls, and scroll their own card-wall content. The shared navbar is mounted inside each scrollable tab wall, so it remains clickable at the top and scrolls out of view naturally as the user scrolls. AI测评, login ↔ choose strips, and choose ↔ assessment card flights keep their established behavior.

Navigation revision (2026-09-04, revised 2026-09-05): the homepage CTA is AI测评 and always scrolls into the standalone centered login card on `#94546c`; it does not check a demo session or skip directly to CHOOSE. The original pink form design remains unchanged; hide the page scrollbar and keep 返回首页 in the top-right. The main nav includes an English Account item; its separate `#account` screen follows the homepage-derived card-wall style and is distinct from CENTER > 账号设置 (`#center/settings`). Home → choose turns the three.js shell to its flat pose first, then pulls three bands (outer bands together, middle opposite); choose → home pulls the bands in reverse, and only after they land does the same shell rotate back to case playback. Login ↔ choose also uses card-whitespace strips; choose ↔ assessments uses staggered whole-card flight with reverse directions. Cases, Forum, Account, CENTER and detail routes change immediately. Cases and Forum use separate twelve-card placeholder walls. CHOOSE and CENTER cards must not show a selected outline or top-right check badge. Each CENTER card opens a blank full-screen detail in that card's sampled color (`#center/<id>`) with only a back control. Additional source-derived crop images belong under `public/assets/crops/`.

Personal-center revision (2026-09-04): the choose screen's 个人中心 card now opens `#profile` (ProfileHub) with the same card-flight transition as assessments; back returns to choose. The page follows the source art `public/assets/profile-reference.png` (1672 × 941): white bounce lettering "CENTER" and five category cards (我的组织 / 我的作品 / 测评记录 / 我的收藏 / 账号设置), all selectable placeholders with the shared check badge. It shares TEST!'s rhythm exactly like CHOOSE!: same 1672×941 unit formula, lettering height 188 at y=122 (aspect kept from the crop, centered), cards 420 tall starting y=343, radius 22, row span 1502 with uniform card widths (the five-across fit), height-filled center-cropped card art. Card-flight and strip seams recognize `.profile-card` / `.profile-wordmark`. Sections are not yet functional (status line 即将开放).

Conversation polish round (2026-09-04): user chat bubbles use dark text (#1f2a3c) on a light assessment-tinted bubble — never white-on-color. Avatars pin to the bubble's first line (`align-items: flex-start`); they must never slide with streaming text or jump when `is-feedback` lands. The thread auto-follows only while the reader is already near the bottom (90px), so looking back is never fought by the stream. All AI output (chat bubbles and practical agent output) renders through `src/markdown-lite.jsx` — bold/italic/code inline, bullet/numbered lists, headings and quotes, built as React nodes (never injected HTML; XSS test included). Reasoning models think in silence: after 2.5s without a content token the pending bubble shows 正在思考…. One completed AI reply finishes a conversation level: a labeled dashed 完成本关 banner appears (TaskAction) so completion is unmissable; never require multiple exchanges to clear a level. embed-runtime guards each frame callback with try/catch (one scene exception must not kill the rAF chain = frozen black screen), and CaseScreen re-asserts the current activation state every 1.2s while a scene layer is visible — healing lost activation messages and stale warm-up frames (conbini black screen on the right face, including after returning from login). Hero wordmark nudge raised to 30 design px.

Polish round (2026-09-04): the home↔login pan/scale is driven by `--turn-p` — the same eased rAF clock that rotates the WebGL shell (responsive.css `.cube-position` interpolates home→flat pose; the old parallel CSS transition is gone) — and zero-duration moves draw synchronously so reduced-motion users and vsync-throttled webviews still land exactly. The embed runtime repaints immediately when `aiquos:visibility active=true` arrives (`__showcaseFrameTime=0`), so a scene warmed while hidden can never come on-screen as a frozen black frame (conbini right-face black screen). Next-group preload layers mount 900ms after the reveal/swap settles, keeping iframe startup off the animation frame. Mario's top-face crop anchors bottom (`object-position: 50% 100%`) so the sky takes the letterbox cut instead of the ground. Hero wordmark nudged right 16 design px (`BRAND_SHIFT_X` in layout.js); nav is Home / Cases / Forum (Cases restarts the carousel, Forum opens the community modal). The login surface was redesigned to the brand system: pale-pink gradient screen, AIQUOS · PLAYGROUND kicker, pill fields with pink focus rings, gradient pink submit — same no-validation demo contract, inputs keep native 16px sizing.

Latest conversation-readability revision (2026-09-04): retain the purple TEST! shell and white guide art, but treat the supplied post-reply screenshot as the source for the conversation task. On desktop, the conversation-only work panel must be `min(84.4vw, 1120px)` wide, `min(72dvh, 632px)` high and use the larger reference rhythm; do not shrink the other objective/practical panels. Use bundled open-source Noto Sans SC (SIL OFL) as the Chinese UI face with sensible macOS/Windows fallbacks. Conversation title/body/composer copy must be darker and larger than the old compact task UI. A completed streamed reply gets the readable `is-feedback` surface, while the chat area scrolls and automatically follows the newest reply so output never overlaps or displaces the fixed composer. Keep the narrow-screen version in normal document flow with a bounded scrollable thread.

Latest cube-shell revision (2026-09-04): the login turn is rendered by three.js as one continuous calibrated 3D mesh (`src/cube3d.js` pure math + `src/cube-scene.js` rasterizer) instead of three independently homography-warped DOM quads — shared edges stay watertight at every progress, so no more mid-turn seams or corner wedges. The resting pose still reproduces the photograph pixel-for-pixel via a (1-p)^2 vertex calibration; the flat pose lands on the exact previous rectangle. Live screens remain DOM planes but are driven each frame by the same projection (`screenQuad`), so they cannot drift off the shell. If WebGL is unavailable the previous DOM projection path takes over (`data-shell="dom"`). three.js ships as an async chunk and is a runtime dependency.

Boot fallback (2026-09-04): the cold-home curtain also lifts after a bounded 8s wait. Some embedded webviews throttle vsync for the whole page (or only for iframes) when their pane is occluded, so scene frames can never arrive there; fast visible environments still open on real frames exactly as before.

Latest AI-task revision (2026-09-04): the conversation assessment uses the DeepSeek Chat Completions API through the server-only route `/api/deepseek/chat` with model `deepseek-v4-flash-vision-exp`; stream text into the last AI bubble and never expose `DEEPSEEK_API_KEY` with a `VITE_` prefix, browser storage or committed file. Image-enabled practical levels use the supplied Ark/OpenAI-compatible image endpoint through `/api/ark/images` with model `doubao-seedream-5-0-pro-260628`; use the server-only `ARK_API_KEY` exactly as in the vendor integration guide. Local development reads ignored `.env.local`; any published Worker must receive both secrets through deployment configuration. The first practical level is now the supplied weekly-report prompt-writing task: present its delivery criteria and a collapsible original spoken report in the left panel, a streamable AI result in the right panel, and one bottom prompt composer. Preserve the orange TEST! shell and complete the task only after an AI result exists.

Latest task-screen fidelity revision (2026-09-04): the three supplied 1672 × 941 screenshots are the visual truth for the task surfaces. At that viewport, preserve the TEST! wordmark at the upper left, the five-node counter at the upper right, and a white panel at approximately x=272, y=249, w=1117, h=632. Objective green uses four full-width pale answer rows, a green outline on the selected row, and a circular lower-right action. Conversation purple uses a left/right/left bubble rhythm and one full-width bottom composer. Practical orange uses a two-column workspace inside the panel and one full-width bottom composer. Keep the real localized task content but do not add secondary dashboard chrome. The existing two-character guide art must render as clean white transparent line art over the panel edge; never expose its original green matte.

Latest assessment-flow direction (2026-09-03): after the TEST! card selection, each assessment opens a dedicated five-stage map and then a standalone full-screen task surface. The visual source is the user-selected orange assessment mock: pure category-color background, existing white bespoke TEST! wordmark at top left, a five-node white progress trail at top right, and exactly one large white rounded operation surface. Retain only the small pair of original IP guides (generated asset `public/assets/assessment-guides-crop.png`), perched over the panel; do not add dashboard rails, grids, extra cards, dark backdrops, or generic illustration clutter. Objective uses answer choices, conversation uses an editable AI chat, and practical uses a compact Agent plan/composer. Comprehensive (blue) must use the same visual shell while mixing the other three interaction modes across its five levels — never reduce it to a separate three-icon summary page. Completing a level returns to the map with the next level unlocked; use restrained panel/node entrance motion and honor reduced-motion.

Latest showcase-loading revision (2026-09-03): remove the pixelated pirate from all carousel groups and controls. On a cold homepage visit, render the main homepage only after the three initially visible cases have a decoded video frame or two rendered scene frames; do not mount next-case preloads until that point. On later changes retain the complete outgoing case until the requested replacement is ready, including if the replacement fails. The reference screen itself is the crop: Wing It video and Mario must fill their display areas without letterboxing.

Latest selection-layout revision (2026-09-03): CHOOSE! must share TEST!'s vertical rhythm at the reference viewport: title height 188px at y=122 and complete card row height 420px at y=343. Keep the three source-derived card artworks proportionate; center the narrower three-card row rather than enlarging it beyond TEST's card height.

Choose layout preference (2026-09-03, tightened after the first pass): the choose screen must match the assessments screen's vertical rhythm in BOTH modes. Wide: the 863px source art alone puts cards at 58% of the page height (vs 45% on assessments), so scale against a virtual 1113px design height — `getChooseLayout` uses `min(width/1822, max(height,560)/1113)`, `.choose-layout` starts at `180u` plus centering on `1113u`, grid margin 33u, 60px bottom padding. Compact (<760px): reuse the assessment values (wordmark `min(76vw, 420px)`, gap 16, margin-top 32, radius 12, padding-bottom 36) with top padding `calc(106px + 0.0877 * min(76vw, 420px))` to compensate the flatter CHOOSE! wordmark (837/175 vs 822/244) so both pages start card rows on the same line. The three-band strip seams are measured live, so they follow automatically.

Latest choose-step revision (2026-09-03, supersedes the direct login→assessment flow): login submit now opens the CHOOSE! page built from `public/assets/choose-reference.png` (1822 × 863) — three real buttons cropped from the art (测试闯关 / 报告查询 / 个人中心), same SourceCrop + wordmark technique as the assessments screen. Clicking 测试闯关 opens the TEST! assessment page; the assessment back button returns to choose, not home. login → choose keeps the three-band strip pull at the choose-card whitespace; choose ↔ assessments flies whole cards (old cards out, new cards in, staggered); 返回首页 runs the strips in reverse and only releases the flat login surface after they land so the cube can rotate home. Choose cards mirror assessment-card interaction: selection state, check badge, status line, disabled while a transition runs. 报告查询/个人中心 stay selectable-but-closed placeholders.

Latest recovery note (2026-09-04): CHOOSE card art must use `SourceCrop`'s supplied source offsets unchanged. Never add centering or translation rules to `.choose-card .source-crop img`, because that hides the first two cards after the login transition. The homepage carousel remains four real cases with a 15-second interval; its initial visible trio must still gate initial preloads.

Latest login form revision (2026-09-03, supersedes “no inputs yet” below): add account and password inputs plus a login submit button inside the front-facing pale-pink screen. Reveal them only after rotation settles. No validation or real authentication: blank fields are allowed, submit clears the form and navigates to the CHOOSE! selection step (see above). Never persist, log, serialize or transmit entered credentials. Keep case playback paused, source shell and 900 ms rotation unchanged. Preserve the existing local preview and hosting metadata; do not publish to a new address as part of this local iteration.

Current login direction (2026-09-03, supersedes the earlier entry flow): remove the content-editing Studio dialog and its visible triggers. Homepage login must rotate the existing cube into a single front-facing 2D surface; do not open assessment selection directly and do not build login inputs yet. Stop every case animation immediately when turning starts and use pale-pink screen faces. Turn and centering share a smooth 900 ms ease-out. Preserve source shell materials but carefully mask corners; no corner wedges or open seams. Reverse restores the homepage cases. The existing TEST! assessment page remains available separately for later integration.

Current carousel direction (2026-09-03): preload the next three-face group while the current group plays. Swap only after an actual rendered scene frame or decoded video frame is ready; keep the old frame during loading. Use at most two layers per screen, pause hidden preloads after two warm-up frames, and pause all media during cube rotation. Preserve the existing 15-second showcase interval unless the user changes it.

Latest transition revision (2026-09-03): do not divide the viewport into equal thirds. Place both horizontal seams in the whitespace above and below the assessment cards, so the complete card row moves intact in the middle band. On phones preserve both rows together. Use these same measured boundaries for outgoing/incoming bands and for the reverse transition.

Latest heading revision (2026-09-03): supersede the YooO! heading below with the bespoke TEST! lettering from `public/assets/test-wordmark-reference.png`. Preserve its exact contours and spacing using source-derived transparency, not a generic font. Leave the four assessment cards and three-band transition intact.

Second-screen direction (2026-09-03): reference `public/assets/assessment-reference.png` governs the black assessment selection screen. Preserve the supplied YooO! graphic and all four card titles/subtitles until the user requests different copy. Clicking the homepage login button is a prototype navigation action, not real authentication. Split the actual current page into three horizontal visual bands (outer bands one direction, center the opposite) with matching incoming bands. Preserve the homepage/custom media state on return; pause hidden media and never mount extra live WebGL scenes for the animation. Keep desktop single-row cards and phone two-column layout. Card selection is real UI state; assessment questions/scoring are not in scope yet.

Product positioning (2026-09-03): this website tests users' ability to use AI, rather than offering generic learning tools. Hero copy should speak about practical tasks, prompting and turning ideas into finished work. Keep a title and short description inside each case screen, but make it quiet and integrated: compact translucent backing, no full-width dark banner, no status dot or prominent border. The assessment flow/scoring is not implemented; do not label the case-viewing CTA as a working test.

User preference (2026-09-03, cases revision): remove the 50k+/120+/98% footer and 10k+ social proof entirely. The cube defaults to automatic rotation through real projects copied from `/Users/lunaecho/Desktop/项目合集_2026-08-31`. Keep originals untouched. Use the Wing It finished Chinese recap, Mario, the existing conbini v2, and both pirate variants. Conbini v1 is mentioned in the collection notes but its folder is absent; do not invent it. Preserve fullscreen responsiveness and independent screen replacement; custom edits suspend the carousel.

Proportional 16:9 design stage (2026-09-18, latest, supersedes the viewport-fill preference below): every element occupies a fixed share of one 16:9 composition, and the composition is uniformly scaled to the window instead of being laid out from the window's own dimensions. The frame is a fixed 1920×1080 box in `src/stage.js` / `src/DesignStage.jsx`; a window that is not 16:9 is letterboxed, never stretched. `--vw`/`--vh` and the rest of the unit variables are redefined *inside* the stage to mean 1% of the frame (19.2px / 10.8px), so a `px` length written in the composition is proportional by construction — that is what makes the proportions hold, rather than a clamp per element. `useStageSize()` hands the frame (never the window) to `layout.js`, `cube-geometry.js`, `assessment-layout.js`, `choose-layout.js`, `profile-layout.js` and every hub. Compact is exempt and keeps the fluid window-based rules it already had: a 16:9 composition scaled to a phone's width would be unreadable. Transition layers (`split-transition`, `card-transition`, `slide-transition`, `transitions`) measure the frame through `getStageSize()`; measurement helpers that read `getBoundingClientRect` divide through by `getFrameScaleFromDom()`, because screen pixels inside the scaled stage are design pixels times the frame scale. Do not write `vw`/`vh` back into an inline style inside the stage, and do not apply a `scale()` on any other ancestor of the layout — either would reintroduce the window as a second unit and put measured geometry out by the frame scale. Cases poster geometry (2026-09-18): the featured stamp is 420×525 design px, centred between the two display words by construction (`top: calc(49.78% - width * var(--poster-scale) / 1.6)`), and the two corner peeks are 0.56× that size, anchored to the frame's edges and deliberately cut off by them (`translateX(∓73%)`, `top/bottom: -6%`) so only a sliver of each shows — the reference frames show the corner stamps half outside the picture. The feature box's aspect is 0.8, the art's true ratio (1122×1402); a `height` and a `width` must not both be set on it, or `aspect-ratio` is ignored and the perforated frame is squeezed out of shape.

User preference (2026-09-03): fill the available browser viewport responsively. Never contain the entire 1536 × 1024 composition inside a centered fixed-aspect-ratio canvas. Anchor navigation/footer to the viewport; scale the original wordmark and cube independently without distortion. Keep existing screen replacement behavior. Very short windows may scroll to preserve readable controls. **SUPERSEDED on 2026-09-18 by the proportional 16:9 design stage above** — the composition is now exactly that centered fixed-aspect-ratio frame. The parts of this note that still hold: artwork is never distorted (one uniform scale, not two axes), screen replacement behaviour is unchanged, and very short windows still scroll in compact.

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

Latest cases-archive direction (2026-09-15): treat the supplied Xiaohongshu video as the motion source of truth for `#cases`. Case changes are a continuous scroll/swipe-driven scene transition: the outgoing stamp enlarges and exits diagonally toward the upper-left while the incoming stamp enters from the lower-right, giant words travel with each scene, and the next color field wipes in horizontally. The dotted route is transient—hide it during movement and draw it only after a case settles. Keep all 36 cases accessible through the index, and let every centered stamp expand into an immersive detail view and reverse cleanly back to the same case. Preserve full-viewport responsiveness, keyboard/touch controls, focus visibility, and a non-spatial reduced-motion fallback.

Cases interaction refinement (2026-09-15): the settled route must trace along its curve as round dots, never reveal by horizontally clipping a raster or expose half-dashes at the drawing head. Keep the featured stamp visibly dominant, including in short embedded-browser viewports where viewport-height caps previously made it too small. Browsing cannot depend on a wheel alone: retain scroll/trackpad and tappable neighbor stamps, and also provide explicit previous/next controls, touch swipe, mouse/pen drag, Arrow/Page keys, Space/Shift+Space, and a concise visible interaction hint.

Cases library correction (2026-09-15): source-frame study supersedes the earlier round-dot/draw-on-settle interpretation. The route is a chain of small yellow rectangular dashes. It is complete while a scene is stable, then retracts from the featured-card end and remains briefly as a short tail beneath the outgoing upper-left stamp; the incoming scene has no route until it settles. Keep only six curated projects in the primary reference-style journey and expose the complete 36-item collection through a distinct CASE LIBRARY. A library detail must return to the library, while a featured detail returns to the featured journey.

Cases real-project revision (2026-09-15, latest): the primary `#cases` journey is the same four real projects used on the homepage — Wing It, 超级马里奥, 日式便利店, and 暴风雨海盗船 — rather than fictional archive placeholders. Their stamp windows are live local media, and opening a featured stamp expands into the actual playable video/scene with a close route back to the same carousel position. Keep the prior 36-item editorial collection inside CASE LIBRARY; a library detail still returns to that library.

Cases deferred-media revision (2026-09-15, latest): featured postage stamps must render one representative local WebP cover per case and must not mount a video, iframe, canvas, or WebGL scene. The real Wing It video and the Mario, conbini, and pirate scenes are created only after the user opens the centered stamp; closing the detail disposes that media and returns to the same carousel position. Covers live in `public/assets/case-covers/` and are 640×480 source-derived stills.

Cases primary preservation revision (2026-09-15, latest): never replace the original six featured editorial cases when adding real projects. The primary journey contains ten cards in this order: the original six archive cases, then Wing It, 超级马里奥, 日式便利店, and 暴风雨海盗船. All ten use static cover images in the postage view. Original cases open their existing `CaseDetail`; only the four real-project cards instantiate `CaseScreen` on open. CASE LIBRARY continues to contain the full 36-item archive and keeps its return-to-library behavior.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

Build app UI in `src/`. Keep `.openai/hosting.json`, `worker/index.js`, `scripts/prepare-sites-build.mjs`, and `tests/sites-worker.test.mjs` intact so the same local prototype can be handed to Sites. Before a Sites handoff, run `npm run build` and `npm run test:sites`; the build must leave `dist/client/index.html`, `dist/server/index.js`, and `dist/.openai/hosting.json`.
